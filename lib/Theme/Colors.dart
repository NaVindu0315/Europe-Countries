import 'package:flutter/material.dart';

// Primary Color (App Bar Color)
const appBarColor = Color(0xFF3498db);

// Background Color
const backgroundColor = Color(0xFFF7F7F7);

// Card Background Color
const cardBackgroundColor = Color(0xFFFFFFFF);

// Accent Color
const accentColor = Color(0xFF8bc34a);

// Text Color
const textColor = Color(0xFF333333);

// Optional Gradient
Gradient gradient = const LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [Color(0xFF3498db), Color(0xFFF7F7F7)],
);
